{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "catalog.published",
  "title": "catalog.published",
  "description": "The online auction catalog for a session has been published. Produced by the Pre-Auction Catalog Service (2); consumed by the Auction & Bidding Service (3).",
  "type": "object",
  "additionalProperties": false,
  "required": ["eventId", "sessionId", "basketIds", "totalBaskets", "occurredAt"],
  "properties": {
    "eventName": { "type": "string", "description": "Optional self-describing event type; equals the topic name. Not required." },
    "eventId": { "type": "string", "format": "uuid" },
    "sessionId": { "type": "string", "minLength": 1 },
    "basketIds": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "totalBaskets": { "type": "integer", "minimum": 1 },
    "occurredAt": { "type": "string", "format": "date-time" }
  }
}
