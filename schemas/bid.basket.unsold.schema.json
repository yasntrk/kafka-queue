{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "bid.basket.unsold",
  "title": "bid.basket.unsold",
  "description": "A basket could not be sold in the current round. Produced by the Auction & Bidding Service (3); typically triggers a re-auction round.",
  "type": "object",
  "additionalProperties": false,
  "required": ["eventId", "sessionId", "basketId", "reason", "occurredAt"],
  "properties": {
    "eventName": { "type": "string", "description": "Optional self-describing event type; equals the topic name. Not required." },
    "eventId": { "type": "string", "format": "uuid" },
    "sessionId": { "type": "string", "minLength": 1 },
    "basketId": { "type": "string", "minLength": 1 },
    "reason": { "type": "string", "enum": ["NO_BIDS", "BID_BELOW_BASE", "WITHDRAWN"] },
    "occurredAt": { "type": "string", "format": "date-time" }
  }
}
